# Idea

I want to be able to do # to reference a file name, from the Terminal declared project locations.

Not sure of the best approach, but basically doing # in a chat should trigger a call from Server > Bridge, and a method would start to list files / folders available, filterable by name.

Also we would need an option to like /project to select on which Terminal project the search would happen