# Feature

Implement a token saver mode that would:
- Very restrict an agent to start using tools it was not asked. E.g. in OpenCode, I asked to read a file to under a problem, and it started making many tool calls in the folder for other files, burning a lot of tokens.
- Approval for commands (but this should be a default feature anyway)
- What else do you suggest?